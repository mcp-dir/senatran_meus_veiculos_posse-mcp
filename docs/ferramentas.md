# Ferramentas

SENATRAN: Meus Veículos - Possuidor (Listagem) expõe 1 ferramenta (todas somente leitura).

### 1. `senatran_meus_veiculos_posse_consultar`
**Input**: `cnpj` (opcional), `pagina` (opcional), `login_cpf` (opcional), `login_senha` (opcional), `pkcs12_cert` (opcional), `pkcs12_pass` (opcional)

SENATRAN: Meus Veículos - Possuidor (Listagem), consulta em fonte oficial.

## Prompts de exemplo

```
Consultar SENATRAN: Meus Veículos - Possuidor (Listagem)
```
